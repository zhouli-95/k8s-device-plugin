apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../deployments/kustomize/base

patches:
  - path: cdi.yaml
    target:
      kind: DaemonSet
      name: device-plugin