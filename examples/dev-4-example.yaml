apiVersion: v1
kind: Pod
metadata:
  name: dev-pod-4
spec:
  containers:
  - name: app
    image: ubuntu:24.04
    command: ["bash", "-c", "sleep 3600"]
    resources:
      limits:
        example.com/dev: "4"
  terminationGracePeriodSeconds: 0
