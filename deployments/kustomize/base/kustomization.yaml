apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: device-plugin

resources:
  - namespace.yaml
  - configmap.yaml
  - daemonset.yaml

images:
  - name: device-plugin
    newName: device-plugin
    newTag: dev
